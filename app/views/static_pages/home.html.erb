<h1>Flickr Get</h1>

<p>
  Input a user's Flickr ID to display their photostream here. You can find it 
  in the URL of their photostream page.
</p>
<p>
  You may also input a username for users who have implemented a "friendly"
  URL.
</p>

<%= form_tag "/", method: :get do %>
  <%= label_tag :id, "Flickr user ID:" %>
  <%= text_field_tag :id %>
  <%= submit_tag "Submit ID" %>
<% end %>

<% unless params[:id].blank? %>
  <% cache ("photos_cache_#{params[:id]}") do %>
    <%= render_photos(params[:id]) %>
  <% end %>
<% end %>
